lm-eval
sacrebleu
sqlglot
evaluate
nltk
langdetect
# lm-eval's IFEval task imports this at MODULE level (tasks/ifeval/instructions_util.py), so
# without it `--tasks ifeval` dies with ModuleNotFoundError before a single prompt is scored.
# It is not a declared dependency of lm-eval, and nothing surfaces it until the task is loaded:
# the gate's probe job burned its full 3-hour walltime on 2026-09-13 and produced nothing.
# IFEval is the control that separates directional loss from general instruction-following, so
# the probe cannot be skipped.
immutabledict
# unbabel-comet is NOT here: it pins transformers<5 against the lock's 5.14.1. It lives in
# the second env built by env/setup_score_env.sh (COMET-22 scoring only).
sqlparse
pypdf   # read the papers in papers/ without a poppler install
