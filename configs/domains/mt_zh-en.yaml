# MT cell: mt_zh-en. Both training directions of a pair are separate cells by design (plan RQ2 —
# Zhu et al. 2024 report collapse for X->en tuning but not en->X, so the asymmetry is the
# moderator test and must be run, not assumed).
_extends: _mt_base.yaml
